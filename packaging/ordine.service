[Unit]
Description=Ordine pipeline service (web UI + workers)
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/ordine serve
Restart=on-failure
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=default.target
